# 设计文档审查 Agent 说明文档

## 项目概述

本项目是一个 **GitHub Actions CI 检查工具**，在开发者提交 Pull Request 时，自动调用 Claude AI 对比 PR 变更与设计/需求文档的一致性，并将审查结果以评分和建议的形式回复到 PR 评论区。

---

## 工作原理

```
开发者提交/更新 PR
        ↓
CI 触发 → 解析 PR 描述中的文档链接
        ↓
抓取设计文档内容 + PR Diff
        ↓
调用 Claude 分析对齐度（0–10 分）
        ↓
将评分 + 改进建议发布为 PR 评论
        ↓
score >= 阈值 → CI 通过（绿色）
score <  阈值 → CI 失败（阻止合并）
```

---

## 部署步骤

### 第一步：将本仓库推送到 GitHub

workflow 文件必须位于默认分支（`main`）才能生效。

```bash
git push origin main
```

### 第二步：添加 Anthropic API Key（必须）

进入：**仓库 → Settings → Secrets and variables → Actions → Secrets**

| 名称 | 值 |
|---|---|
| `ANTHROPIC_API_KEY` | 从 [console.anthropic.com](https://console.anthropic.com) 获取的密钥 |

### 第三步：设置评分阈值（可选，默认值为 7）

进入：**仓库 → Settings → Secrets and variables → Actions → Variables**

| 名称 | 值 |
|---|---|
| `SCORE_THRESHOLD` | 整数，范围 0–10，例如填写 `7` |

修改后立即生效，无需重新部署。

### 第四步：授予 Workflow 写评论权限

进入：**仓库 → Settings → Actions → General → Workflow permissions**

选择 **"Read and write permissions"** → 点击 Save

### 第五步：配置分支保护规则（强制审查通过才能合并）

进入：**仓库 → Settings → Branches → Add branch protection rule**

- Branch name pattern：`main`
- 勾选：**"Require status checks to pass before merging"**
- 搜索并添加状态检查：`design-doc-review`

> **注意**：该状态检查名称需在 CI 至少运行一次后才会出现在搜索列表中。建议先创建一个测试 PR 触发一次。

### 第六步：指向其他仓库（可选）

默认情况下，Agent 审查 workflow 所在仓库的 PR。若需审查其他仓库：

进入：**仓库 → Settings → Secrets and variables → Actions → Variables**

| 名称 | 值 |
|---|---|
| `TARGET_REPO` | `owner/other-repo` |

若为跨仓库访问，还需在 Secrets 中添加具有 `repo` 权限的 PAT（个人访问令牌），并在 workflow 中将 `secrets.GITHUB_TOKEN` 替换为 `secrets.GH_PAT`。

---

## 使用说明

### 在 PR 描述中附上设计文档链接

Agent 会自动提取 PR 描述中所有 `https://` 开头的链接并抓取内容。建议格式如下：

```markdown
## 变更内容
- 新增用户认证流程

## 设计文档
- [需求文档](https://docs.google.com/document/d/YOUR_DOC_ID/edit)
- [设计文档](https://docs.google.com/document/d/YOUR_DOC_ID/edit)
```

### 支持的文档类型

| 类型 | 支持情况 |
|---|---|
| 公开的 Google Docs | ✅ 自动转换为纯文本导出 |
| GitHub Raw 文件 | ✅ |
| 其他公开 URL（Confluence 等） | ✅ |
| 私有 Google Docs / Notion | ❌ 当前 POC 版本不支持（需要 OAuth） |

### 审查结果示例

CI 运行完成后会在 PR 下方自动发布评论，格式如下：

```
## Design Doc Review ✅

**Score: 8/10** (threshold: 7/10)

### Suggestions
- 第 3 节"数据模型"未覆盖新增的 `user_token` 字段，建议补充说明
- 接口变更未反映在 API 文档的版本号中

---
*Posted by Design Doc Review Agent*
```

---

## 评分标准

| 分数 | 含义 |
|---|---|
| 10 | PR 与设计文档完全一致 |
| 7–9 | 存在小差距，需少量文档更新 |
| 4–6 | 存在中等差距，需更新多个章节 |
| 1–3 | 存在重大差距，设计文档严重滞后 |
| 0 | 未找到文档，或 PR 与设计完全矛盾 |

---

## 本地测试

```bash
# 安装依赖
pip install -r requirements.txt

# 设置环境变量
export ANTHROPIC_API_KEY=sk-ant-...
export GITHUB_TOKEN=ghp_...
export PR_NUMBER=1
export REPO_FULL_NAME=owner/repo
export SCORE_THRESHOLD=7

# 运行
python agent/review.py
```

---

## 项目文件结构

```
.
├── agent/
│   └── review.py                        # 核心 Agent 逻辑
├── .github/
│   └── workflows/
│       └── design-doc-review.yml        # GitHub Actions workflow 定义
├── requirements.txt                     # Python 依赖（anthropic, requests）
└── README.md                            # 英文说明（原始）
```

---

## 依赖项

| 依赖 | 版本要求 | 用途 |
|---|---|---|
| `anthropic` | >= 0.25.0 | 调用 Claude API |
| `requests` | >= 2.31.0 | 抓取文档和调用 GitHub API |
| Python | 3.12 | 运行环境 |
