# 测试配置指南

## 必须配置

**GitHub Secrets（仓库 Settings → Secrets and variables → Secrets）**

| Secret 名称 | 值 |
|---|---|
| `RAKUTEN_AI_GATEWAY_TOKEN` | `raik-pat-...`（你的 Rakuten PAT） |

> `GITHUB_TOKEN` 不需要手动配置，GitHub Actions 自动提供。

---

## 可选配置

**GitHub Variables（Settings → Secrets and variables → Variables）**

| Variable 名称 | 说明 | 默认值 |
|---|---|---|
| `SCORE_THRESHOLD` | 通过分数线 | `7` |
| `TARGET_REPO` | 若要审查其他仓库的 PR（格式 `owner/repo`） | 当前仓库 |

---

## 新仓库部署

如果在新仓库测试，需复制以下两个文件：

```
.github/workflows/design-doc-review.yml
agent/review.py
```

依赖由 workflow 自动安装（`pip install anthropic requests`），无需额外操作。

---

## 测试方法

配置好后，向该仓库提一个 PR，在 PR 描述中粘贴一个文档链接（如 Google Docs 或任意 public URL），CI 会自动触发并在 PR 下发评论。
