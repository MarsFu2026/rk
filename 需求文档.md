# 需求文档：设计文档审查 Agent

**版本**：v1.0  
**日期**：2026-05-20  
**状态**：草稿

---

## 1. 背景与目标

### 1.1 背景

在软件研发流程中，PR（Pull Request）与设计/需求文档之间往往存在脱节：代码已合并，但文档仍停留在旧版本。人工审查耗时且容易遗漏，导致文档长期滞后于实现。

### 1.2 目标

在 GitHub PR 合并流程中引入 AI 自动审查机制，通过 Claude 对比 PR 变更内容与关联设计文档，输出对齐评分和改进建议，从而：

- 降低文档滞后风险
- 减少人工 Code Review 中文档对齐的重复劳动
- 建立可量化的"代码-文档一致性"质量门禁

---

## 2. 用户角色

| 角色 | 描述 |
|------|------|
| **开发者** | 提交 PR、在 PR 描述中附上设计文档链接的人 |
| **团队负责人** | 配置评分阈值、管理分支保护规则 |
| **仓库管理员** | 配置 Secrets、Workflow 权限、Runner 环境 |

---

## 3. 功能需求

### 3.1 核心功能

#### F1 — 自动触发审查

- **触发条件**：PR 被创建（`opened`）或更新（`synchronize`）时自动触发
- **触发范围**：默认审查当前仓库的 PR；可通过 `TARGET_REPO` 变量指向其他仓库

#### F2 — 文档链接解析

- 自动提取 PR 描述正文中所有 `https://` 开头的 URL
- 支持 Google Docs 公开文档（自动转换为纯文本导出格式）
- 支持任意公开 URL（GitHub Raw 文件、Confluence 公开页等）
- 不支持需要 OAuth 认证的私有文档（当前 POC 限制）

#### F3 — PR Diff 获取

- 通过 GitHub API 获取 PR 的完整 diff
- 截取前 6000 字符，控制 Token 消耗

#### F4 — AI 评分与建议

- 调用 Claude（通过 Rakuten AI Gateway / Bedrock 协议）进行分析
- 输入：PR 描述 + 设计文档内容 + PR Diff
- 输出：
  - `score`：整数，范围 0–10
  - `suggestions`：Markdown 格式的改进建议列表

**评分标准**：

| 分数 | 含义 |
|------|------|
| 10 | PR 与设计文档完全一致 |
| 7–9 | 存在小差距，需少量文档更新 |
| 4–6 | 存在中等差距，需更新多个章节 |
| 1–3 | 存在重大差距，设计文档严重滞后 |
| 0 | 未找到文档，或 PR 与设计完全矛盾 |

#### F5 — PR 评论发布

- 审查完成后自动在 PR 下方发布评论
- 评论内容包含：评分、阈值、改进建议、审查工具标识

#### F6 — CI 状态门禁

- `score >= SCORE_THRESHOLD`：CI 检查通过（绿色），允许合并
- `score < SCORE_THRESHOLD`：CI 检查失败（红色），阻止合并
- 默认阈值为 `7`，可通过仓库变量 `SCORE_THRESHOLD` 调整

---

### 3.2 配置需求

| 配置项 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| `ANTHROPIC_API_KEY` | Secret | 是（直连模式） | — | Anthropic API 密钥 |
| `AWS_BEARER_TOKEN_BEDROCK` | Secret/Env | 是（Rakuten 模式） | — | Rakuten AI Gateway PAT |
| `ANTHROPIC_BEDROCK_BASE_URL` | Env | 是（Rakuten 模式） | — | Rakuten 网关地址 |
| `SCORE_THRESHOLD` | Variable | 否 | `7` | CI 通过所需的最低分数 |
| `TARGET_REPO` | Variable | 否 | 当前仓库 | 跨仓库审查时指定目标仓库 |
| `GITHUB_TOKEN` | 内置 | 自动 | — | 用于 GitHub API 调用及发布评论 |

---

### 3.3 部署需求

1. Workflow 文件必须位于默认分支（`main`）的 `.github/workflows/` 目录
2. 仓库需授予 Workflow **"Read and write permissions"**（用于发布评论）
3. 分支保护规则中需添加 `design-doc-review` 为必须通过的状态检查

---

## 4. 非功能需求

| 类别 | 要求 |
|------|------|
| **响应时间** | 单次审查完成时间 ≤ 60 秒（含文档抓取 + AI 分析） |
| **文档抓取** | 单个文档内容截取上限 8000 字符；单个请求超时 15 秒 |
| **AI 输入** | PR Diff 截取上限 6000 字符 |
| **容错性** | 文档抓取失败时跳过该文档，不中断整体流程 |
| **安全性** | API Token 通过 Secrets 注入，不得硬编码在代码或 YAML 中 |
| **可观测性** | 支持 OpenTelemetry（OTEL）遥测数据上报至 Rakuten 内部平台 |

---

## 5. 系统架构

```
开发者提交/更新 PR
        │
        ▼
GitHub Actions 触发 Workflow
        │
        ├─ 获取 PR 描述（GitHub API）
        ├─ 解析文档链接（正则提取）
        ├─ 抓取文档内容（HTTP GET）
        ├─ 获取 PR Diff（GitHub API）
        │
        ▼
  调用 Claude（Rakuten AI Gateway）
        │  输入：PR 描述 + 文档内容 + Diff
        │  输出：{ score, suggestions }
        │
        ├─ 发布 PR 评论（GitHub API）
        │
        └─ 设置 CI 状态
              score >= threshold → exit 0（通过）
              score <  threshold → exit 1（失败）
```

---

## 6. 与设计文档的对齐情况

| 需求项 | 实现状态 | 备注 |
|--------|----------|------|
| PR 触发自动审查 | ✅ 已实现 | |
| 文档链接解析 | ✅ 已实现 | |
| Google Docs 支持 | ✅ 已实现 | 仅公开文档 |
| AI 评分（0–10） | ✅ 已实现 | |
| PR 评论发布 | ✅ 已实现 | |
| CI 门禁 | ✅ 已实现 | |
| 私有文档支持 | ❌ 未实现 | POC 阶段限制，需 OAuth |
| 跨仓库审查 | ⚠️ 部分实现 | 需手动配置 PAT |

---

## 7. 约束与限制

- 当前为 **POC（概念验证）版本**，不保证生产环境稳定性
- 不支持私有 Google Docs、Notion 等需要登录的文档平台
- Claude 模型版本固定为 `anthropic.claude-sonnet-4-5-20250929-v1:0`，如需升级需修改代码
- 文档内容和 Diff 均有长度截断，超长内容会被裁剪

---

## 8. 后续迭代方向（参考）

- 支持私有文档（Google Docs OAuth、Notion API）
- 支持评论中提供具体的文档段落修改建议（diff 格式）
- 增加多语言支持（当前建议为英文输出）
- 支持自定义 Prompt 模板
- 将 Agent 逻辑封装为独立 GitHub Action，便于跨仓库复用
